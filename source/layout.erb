<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport"
          content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Use the title from a page's frontmatter if it has one -->
    <title>chase stephens<%= ' - ' + current_article.title unless current_article.nil? %></title>
    <%= stylesheet_link_tag "all", "normalize" %>
    <%= javascript_include_tag "site" %>
  </head>
  <body>
    <div>
      <div>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/posts">Posts</a></li>
        </ul>
        <% if current_article.nil? %>
          <h1><%= current_page.url.gsub('/', '').gsub('-', ' ').capitalize %></h1>
        <% else %>
          <h1><%= current_article.title %></h1>
          <div class="meta">
            <% if current_article.tags.length > 1 %>
              <p><%= current_article.date.strftime('%B %e, %Y') %> | <%= formatted_article_tags(current_article.tags) %></p>
            <% else %>
              <p><%= current_article.date.strftime('%e %B %Y') %></p>
            <% end %>
          </div>
        <% end %>
      </div>
    <%= yield %>
  </body>
</html>
